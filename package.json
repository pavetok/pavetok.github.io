{
  "name": "pavetok.github.io",
  "scripts": {
    "build": "npx eleventy",
    "start": "npx eleventy --serve",
    "puml": "java -jar plantuml.jar -svg -nometadata -x diagrams/styles.puml -o src/_includes/$ diagrams/**"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-navigation": "^0.3.2",
    "clean-css": "^5.2.1"
  }
}
